<div class="flex items-center gap-4 p-2 rounded-lg cursor-pointer transition-colors {{ selected ? 'bg-gray-800 border-l-4 border-blue-600' : 'hover:bg-gray-800' }}">
    <img src="{{ politician.image }}" alt="{{ politician.name }}" class="w-14 h-14 rounded-lg object-cover" />
    <div>
        <div class="text-white font-semibold">{{ politician.name }}</div>
        <div class="text-gray-400 text-xs">{{ politician.role }}</div>
    </div>
</div> 