<div>
    <select id="{{ name }}" name="{{ name }}" class="w-full bg-slate-800 text-gray-200 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500">
        {% for option in options %}
            <option>{{ option }}</option>
        {% endfor %}
    </select>
</div> 