<div class="flex flex-col p-3 rounded-lg cursor-pointer transition-colors {{ selected ? 'bg-gray-800 border-l-4 border-blue-600' : 'hover:bg-gray-800' }}">
    <div class="flex items-center justify-between">
        <div>
            <div class="text-white font-semibold">{{ partner.name }}</div>
            <div class="text-gray-400 text-xs">Risk: {{ partner.risk }}<br>{{ partner.type }}</div>
        </div>
        <span class="text-gray-400">&#8250;</span>
    </div>
</div> 