<div class="bg-gray-800 rounded-lg p-5 flex flex-col min-w-[180px]">
    <span class="text-gray-400 text-xs mb-1">{{ title }}</span>
    <span class="text-white text-2xl font-semibold">{{ value }}</span>
    <span class="text-xs mt-1 font-medium" style="color: {{ variation_color|default('#22c55e') }}">{{ variation }}</span>
</div> 